<div class="content-wrapper">
  <%= render "/shared/page_header" %>
  
  <section class="content">
      <div class="titles-custom">
        <h1><%= t('Editing quality tip') %></h1>
      </div>
      
      <%= render 'form' %>
      <%= link_to t('Show'), @quality_tip, class: 'btn btn-default' %>
      <%= link_to t('Back'), quality_tips_admin_path, class: 'btn btn-default' %>
  </section>
</div>